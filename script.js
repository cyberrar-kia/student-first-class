// The hero flow diagram animates in via CSS keyframes (see #hero-flow rules
// in styles.css) and respects prefers-reduced-motion automatically.
// No runtime behaviour is required beyond that, so this file intentionally
// stays empty other than this note — kept as a separate file for easy
// extension later without touching markup or styles.
